<Window x:Class="Theoria.Desktop.ArticleViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Theoria – Article Viewer"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <Border Grid.Row="0"
                Background="#2c3e50"
                Padding="10,8">
            <DockPanel>
                <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right">
                    <Button Content="&#x2190; Back"
                            Click="BackButton_Click"
                            Padding="10,4"
                            Margin="0,0,6,0"
                            Background="#34495e"
                            Foreground="White"
                            BorderBrush="#4a6785"
                            Cursor="Hand"/>
                    <Button Content="Open in Browser"
                            Click="OpenInBrowser_Click"
                            Padding="10,4"
                            Background="#34495e"
                            Foreground="White"
                            BorderBrush="#4a6785"
                            Cursor="Hand"/>
                </StackPanel>
                <TextBlock x:Name="UrlDisplay"
                           Foreground="#bdc3c7"
                           FontSize="13"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- Web content -->
        <WebBrowser x:Name="Browser"
                    Grid.Row="1"/>
    </Grid>
</Window>
